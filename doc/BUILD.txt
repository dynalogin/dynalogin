

0. If using a tarball

 skip to step 3

1. Obtain code from git

2. autotools setup

  autoreconf --install
  #autoconf -f && autoheader && automake
  #autoreconf -f --install && autoheader && automake

3. configure

  #./configure CFLAGS="-I /usr/include/apr-1.0"
  ./configure --enable-shared CFLAGS="-I/usr/include/apr-1.0 -g"

  possible alternative for 32 bit build:

  ./configure --enable-shared CFLAGS="-I/usr/include/apr-1.0 -D_LARGEFILE64_SOURCE -g" --prefix=/usr/local 

4. build

  make

5. test

  ./dynalogin_tester testuser

6. install

  make install
 
